{
  "name": "lingochunk-recorder",
  "version": "0.6.1",
  "private": true,
  "description": "Browser extension that records language lessons and uploads them to LingoChunk",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint:firefox": "npm run build && web-ext lint --source-dir dist/firefox",
    "run:firefox": "npm run build && web-ext run --source-dir dist/firefox"
  },
  "license": "MIT",
  "devDependencies": {
    "fake-indexeddb": "^6.0.0",
    "vitest": "^3.0.0",
    "web-ext": "^8.0.0"
  }
}
